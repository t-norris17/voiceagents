# Content Cleaner env (set in the Vercel project settings — NEVER commit real values).
# Powers the rewrite + critic calls (CLI and the /api/clean door).
ANTHROPIC_API_KEY=

# --- Publish pipeline (/api/publish, /api/kb_list) ---
# Supabase: service role key is server-only. Writes/reads the kb_articles table.
SUPABASE_URL=https://rlhybqslnqhggbykjrqg.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
# ElevenLabs Knowledge Base API + the target Robin agent to attach documents to.
ELEVENLABS_API_KEY=
ELEVENLABS_AGENT_ID=
