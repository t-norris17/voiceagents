# Secrets — never commit. Store these in Vercel env vars (or a local .env.local).
.env
.env.*
!.env.example

# Node / Vercel
node_modules/
.vercel/

# Downloaded call transcripts — may contain real caller PII, never commit
transcripts/
**/transcripts/
robin-calls/
